#include <bits/stdc++.h>
using namespace std;

void IngresarNumeros(vector<int>N);
int PrimerElementoPar(vector<int> V);
int main() {
    int n;
    vector<int> V;
   
    cout << "Ingresa la cantidad de números: "<<endl;
    IngresarNumeros(N);
    int resultado=int PrimerElementoPar(V);
    cout<<"el primer numero divisible por 3 es: "<<resultado<<endl;

    return 0;
}
   void IngresarNumeros(vector<int>N) {
      for (int i = 0; i < n; i++) {
        int num;
        cin >> num;
        V.push_back(num);
    }
}

   int PrimerElementoPar(vector<int> V){
   int num;
    for (int i = 0; i < V.size(); i++) {
        if (i %  2== 0 && V[i] % 3 == 0) {
          num+=v[i];
    }
      return num;
   }
 }
